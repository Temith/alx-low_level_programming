#include "main.h"
/**
* main - Entry point
* Descriotion: "check if the integer is positive or negative"
* Return: Always 0 (Success)
*/

void positive_or_negative(int i);
{
	if (i < 10)
	{
		printf("%d is negative", i);
	}
	else
	{
		printf("%d is positive", i);
	}
	printf("\n");

	return (0);
}
