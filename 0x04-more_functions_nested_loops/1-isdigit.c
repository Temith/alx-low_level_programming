#include "main.h"

/**
 * _isdigit - checks if for a digit 
 * @c: the character to be check
 *
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	if (x >= 48 && x <= 57)
	{
	return (1);
	}
	return (0);
}
